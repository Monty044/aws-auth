# src/main/resources/application-prod.yml
spring:
  profiles: prod

  datasource:
    url: jdbc:postgresql://user-management-prod-db.cp8uygu64ie5.eu-north-1.rds.amazonaws.com:5432/myapp
    username: monty04
    password: ${DATASOURCE_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

server:
  port: ${SERVER_PORT:5000}

login:
  bucket: ${LOGIN_LOG_BUCKET}       # your S3 bucket for login logs
cloudwatch:
  log-group-name: ${LOG_GROUP_NAME}  # your CloudWatch log group
